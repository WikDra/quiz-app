# Zmienne środowiskowe dla Flask Quiz-App

# Sekretne klucze (używaj silnych, unikalnych kluczy w środowisku produkcyjnym!)
SECRET_KEY=dev_secret_key_fallback
JWT_SECRET_KEY=dev_jwt_secret_key_fallback

# URL frontendu
FRONTEND_URL=http://localhost:5173

# Konfiguracja Google OAuth (zastąp tymi dostarczonymi przez Google Cloud Platform)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
